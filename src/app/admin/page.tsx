import Link from "next/link";
import { AlertTriangle, CheckCircle2, Database, FileSpreadsheet } from "lucide-react";
import { PageHeading } from "@/components/page-heading";
import { getContent } from "@/lib/content/repository";
export default async function AdminPage(){const {report}=await getContent();return <div className="page-wrap"><PageHeading eyebrow="Owner admin" title="콘텐츠 검수 대시보드" description="관리자 인증과 RLS가 연결된 환경에서만 변경 작업을 허용합니다. 현재 화면은 로컬 이관 산출물을 읽기 전용으로 보여줍니다."/><div className="grid gap-4 md:grid-cols-4"><Metric icon={<Database/>} value={report.rows.canonicalQuestions} label="대표 문제"/><Metric icon={<CheckCircle2/>} value={report.publishedQuestionCount} label="공개 가능"/><Metric icon={<AlertTriangle/>} value={report.reviewQuestionCount} label="검수 대기"/><Metric icon={<FileSpreadsheet/>} value={report.rows.backlog} label="잔여 백로그"/></div><div className="card mt-6 p-7"><h2 className="text-xl font-extrabold">최근 이관 대사</h2><dl className="mt-5 grid gap-4 text-sm md:grid-cols-2"><div><dt className="text-slate-500">원본 체크섬</dt><dd className="mt-1 break-all font-mono">{report.sourceSha256}</dd></div><div><dt className="text-slate-500">수량 대사</dt><dd className="mt-1 font-bold text-emerald-700">{report.exactMatch?'일치':'불일치 — 발행 차단'}</dd></div><div><dt className="text-slate-500">원문 개념 별칭</dt><dd className="mt-1 font-bold">{report.uniqueConcepts}</dd></div><div><dt className="text-slate-500">정규 개념</dt><dd className="mt-1 font-bold">{report.canonicalConcepts}</dd></div></dl><Link href="/admin/imports" className="mt-6 inline-block rounded-xl bg-[#173957] px-5 py-3 font-bold text-white">이관·검수 상세</Link></div></div>}
function Metric({icon,value,label}:{icon:React.ReactNode;value:number;label:string}){return <div className="card p-6"><span className="text-[#16697a]">{icon}</span><p className="mt-5 text-3xl font-black">{value.toLocaleString()}</p><p className="text-sm text-slate-500">{label}</p></div>}

