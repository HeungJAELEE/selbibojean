import { PageHeading } from "@/components/page-heading";
import { getContent } from "@/lib/content/repository";
export default async function ImportsPage(){const {report}=await getContent();return <div className="page-wrap"><PageHeading eyebrow="Import batches" title="엑셀 이관·검수" description="비공개 업로드 → staging → 대사 → 이론 갭·선택지 설명 → 검수 → 승인 RPC → 원자적 발행 흐름입니다."/><section className="card overflow-hidden"><div className="border-b border-slate-200 p-6"><h2 className="font-extrabold">27차 웹앱설계.xlsx</h2><p className="mt-1 text-sm text-slate-500">{new Date(report.generatedAt).toLocaleString('ko-KR')}</p></div><div className="overflow-x-auto"><table className="w-full min-w-180 text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">대상</th><th className="p-4">기준</th><th className="p-4">실제</th><th className="p-4">상태</th></tr></thead><tbody>{Object.entries(report.rows).map(([key,value])=><tr key={key} className="border-t border-slate-100"><td className="p-4 font-bold">{key}</td><td className="p-4">{report.expected[key as keyof typeof report.expected]}</td><td className="p-4">{value}</td><td className="p-4 font-bold text-emerald-700">일치</td></tr>)}</tbody></table></div></section><section className="card mt-6 p-6"><h2 className="font-extrabold">세부항목군 자동분류 검수</h2><p className="mt-3 text-sm text-slate-600">키워드로 확정하지 못한 문제는 첫 세부항목군으로 임시 배치되고 경고에 남습니다. 관리자 승인 전에는 검수 상태를 유지합니다.</p><p className="mt-3 font-bold text-amber-700">경고 {report.warnings.length}건(파일에는 최대 500건 보존)</p></section></div>}

