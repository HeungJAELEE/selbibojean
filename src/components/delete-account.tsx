"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function DeleteAccount(){const router=useRouter(); const [open,setOpen]=useState(false);const [password,setPassword]=useState('');const [error,setError]=useState(''); async function remove(){const response=await fetch('/api/account/delete',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});const result=await response.json();if(!response.ok){setError(result.error);return;}router.push('/');router.refresh()} return <div className="mt-6 border-t border-red-100 pt-6">{!open?<button onClick={()=>setOpen(true)} className="rounded-xl border border-red-300 px-5 py-3 font-bold text-red-700">계정 즉시 삭제</button>:<div className="rounded-xl bg-red-50 p-5"><p className="font-bold text-red-900">계정과 모든 서버 학습기록을 즉시 삭제합니다. 되돌릴 수 없습니다.</p><input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="비밀번호 재확인" className="mt-4 w-full rounded-xl border border-red-200 p-3"/><div className="mt-3 flex gap-2"><button onClick={remove} className="rounded-xl bg-red-700 px-5 py-3 font-bold text-white">영구 삭제</button><button onClick={()=>setOpen(false)} className="rounded-xl bg-white px-5 py-3 font-bold">취소</button></div>{error&&<p className="mt-3 text-sm text-red-700">{error}</p>}</div>}</div>}

