"use client";
import { useState } from "react";
export function AdminLogin(){const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [loading,setLoading]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();setLoading(true);const response=await fetch('/api/admin/magic-link',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const result=await response.json();setMessage(result.message??result.error);setLoading(false)}return <form onSubmit={submit} className="card mx-auto max-w-md p-8"><h1 className="text-2xl font-extrabold">소유자 관리자 로그인</h1><p className="mt-3 text-sm leading-6 text-slate-600">일반 아이디 계정과 분리된 allowlist 이메일로 Magic Link를 받습니다.</p><label className="mt-6 grid gap-2 text-sm font-bold">관리자 이메일<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required className="rounded-xl border border-slate-300 p-3"/></label><button disabled={loading} className="mt-5 w-full rounded-xl bg-[#173957] p-4 font-extrabold text-white">{loading?'전송 중…':'Magic Link 보내기'}</button>{message&&<p className="mt-4 rounded-xl bg-slate-50 p-3 text-sm">{message}</p>}</form>}

